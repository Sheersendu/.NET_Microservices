# .NET_Microservices

## APIS

### Catalog API

1. GET /items
2. GET /items/{id}
3. POST /items
4. PUT /items/{id}
5. DELETE /items/{id}

### Inventory API

1. POST /items - Adds an items to a player's inventory bag
2. GET /items?userId={userId}